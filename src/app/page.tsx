export default function HomePage() {
  return (
    <section>
      <h2 className="text-lg font-semibold">Nothing to measure yet</h2>
      <p className="mt-2 max-w-prose text-ink-soft">
        The materials calculator arrives in Block 1. What is already here is the
        ruler everything else will be measured with: lengths in whole
        millimetres, quantities as exact fractions, and one rounding up, at the
        purchase line.
      </p>
    </section>
  )
}
